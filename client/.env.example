# Only variables genuinely safe to expose to the browser go here.
# Never put database URLs, session secrets, or Supabase service-role keys in VITE_* variables.

VITE_API_BASE_URL=http://localhost:4000/api
